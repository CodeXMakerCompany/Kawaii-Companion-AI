version: "3.8"

services:
  backend:
    build: .
    container_name: electron-live2d-backend
    ports:
      - "12393:12393"
    environment:
      - PORT=12393
    volumes:
      - ./models:/app/models:ro
    restart: unless-stopped
